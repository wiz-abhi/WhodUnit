# Repo "About" — paste sheet

GitHub's About panel is repo *settings*, not files, so it can't be set from code.
Open <https://github.com/wiz-abhi/WhodUnit> → click the **⚙️ gear** next to "About"
(top-right of the repo page) → paste the three fields below → **Save changes**.

---

## Description

```
Deterministic structural root-cause analysis whose output is a SigNoz query you own. Mines the pattern that separates failing traces from healthy ones, compiles it into a verified builder_trace_operator query, and arms it as an alert. No LLM at runtime.
```

*(248 chars — GitHub's limit is 350.)*

## Website

```
https://wiz-abhi-whodunit-replay.static.hf.space
```

*Points at the live interactive replay, so the repo page has a "try it" link
above the fold.*

## Topics

Paste one at a time (GitHub adds them as you type):

```
signoz
opentelemetry
observability
distributed-tracing
root-cause-analysis
clickhouse
query-builder
sre
apm
python
hackathon
wemakedevs
```

## Checkboxes

Leave **Releases**, **Packages** and **Deployments** unchecked — the repo has none,
and empty sidebar sections read as unfinished. Keep them off unless you cut a release.

---

## Optional, 30 seconds each

- **Social preview image** (Settings → General → Social preview): upload
  `docs/blog/images/sketch-wall.png` or `docs/assets/hero.gif`'s first frame. This is
  what renders when the repo link is shared on LinkedIn/X/Slack — worth doing before
  you post about the submission.
- **Pin the repo** on your profile (profile → Customize your pins) so it's the first
  thing a judge or recruiter sees.
